import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NoteTool — SurgicalBrain Medical Synthesis Engine",
  description: "The Universal Medical Synthesis Engine. Dissect. Map. Act. Connect. Built with the Surgeon's Mind philosophy for high-performance medical knowledge management.",
  keywords: ["NoteTool", "SurgicalBrain", "Medical Education", "Active Recall", "Clinical Algorithms", "Knowledge Management"],
  authors: [{ name: "NoteTool Team" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
